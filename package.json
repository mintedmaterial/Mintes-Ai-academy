{
  "name": "mintes-ai-academy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler types && node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "validate": "node scripts/validate.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.113.0"
  }
}
