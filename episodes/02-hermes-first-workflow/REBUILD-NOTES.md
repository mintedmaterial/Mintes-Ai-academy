# Episode 2 rebuild notes

The first public render was too sparse and the YouTube upload remained stuck processing. This replacement uses the blog's instructional command content as synchronized on-screen terminal panels and verification cards.

The detailed scenes show:
- `hermes --version`, `hermes config path`, and `hermes config check` with expected outputs
- a real chat command plus the `hermes doctor` gate
- `hermes skills list` and `hermes skills search "research"` plus skill anatomy
- `hermes profile create research`, `research setup`, and `research chat` with isolated state
- the complete safe cron command, followed by list, run, and artifact inspection
- a final release checklist

The prior master remains preserved. The rebuilt master is `renders/episode-02-hermes-first-workflow-rebuilt.mp4`.

SHA-256: da6f0453c52a671487a9d59cc60767594b4895af05d4928bd64393610f21d16d
Bytes: 9788553
