# Thumbnail notes

Primary: `thumbnail.svg` at 1280x720.

Headline: BUILD AN AI IMAGE APP

Hierarchy: Cloudflare is primary because this is a Cloudflare episode. Hermes is secondary because Hermes is the build and documentation system behind the project.

Before publication, create a captured-image variant using a real Minte Image Lab result as the right-side visual. Keep the headline and logo layers deterministic. Do not ask an image model to redraw either logo.

Suggested alternate headline: `WORKERS AI IN 10 MINUTES`
